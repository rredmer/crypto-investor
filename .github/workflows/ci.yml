name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - name: Install backend dependencies
        run: |
          python -m venv backend/.venv
          backend/.venv/bin/pip install -e "backend[dev]" --quiet
      - name: Ruff lint
        run: backend/.venv/bin/ruff check backend/core/ backend/portfolio/ backend/trading/ backend/market/ backend/risk/ backend/analysis/ backend/tests/ common/ nautilus/ hftbacktest/ research/

  lint-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: cd frontend && npm ci
      - name: ESLint
        run: cd frontend && npx eslint .
      - name: TypeScript type check
        run: cd frontend && npx tsc --noEmit -p tsconfig.app.json

  test-backend:
    runs-on: ubuntu-latest
    needs: lint-backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - name: Install backend dependencies
        run: |
          python -m venv backend/.venv
          backend/.venv/bin/pip install -e "backend[dev,analysis]" --quiet
      - name: Run pytest
        env:
          DJANGO_DEBUG: "true"
        run: backend/.venv/bin/python -m pytest backend/tests/ -v

  test-frontend:
    runs-on: ubuntu-latest
    needs: lint-frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: cd frontend && npm ci
      - name: Run vitest
        run: cd frontend && npx vitest run
      - name: Check bundle size
        run: |
          cd frontend && npm run build
          GZIP_SIZE=$(find dist -name '*.js' -o -name '*.css' | xargs gzip -c | wc -c)
          echo "Gzipped bundle size: $((GZIP_SIZE / 1024))KB"
          if [ "$GZIP_SIZE" -gt 512000 ]; then
            echo "::error::Bundle size ${GZIP_SIZE} exceeds 500KB limit"
            exit 1
          fi

  security-scan:
    runs-on: ubuntu-latest
    needs: [lint-backend, lint-frontend]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install backend dependencies
        run: |
          python -m venv backend/.venv
          backend/.venv/bin/pip install -e "backend[dev]" --quiet
          backend/.venv/bin/pip install pip-audit --quiet
      - name: pip-audit (Python)
        run: backend/.venv/bin/pip-audit -r backend/pyproject.toml --desc || true
      - name: npm audit (Node)
        run: cd frontend && npm ci && npm audit --audit-level=high || true

  schema-freshness:
    runs-on: ubuntu-latest
    needs: [lint-backend, lint-frontend]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: |
          python -m venv backend/.venv
          backend/.venv/bin/pip install -e "backend[dev]" --quiet
          cd frontend && npm ci
      - name: Check schema freshness
        env:
          DJANGO_DEBUG: "true"
        run: |
          backend/.venv/bin/python backend/manage.py spectacular --file /tmp/schema-check.yaml 2>/dev/null
          diff -q frontend/schema.yaml /tmp/schema-check.yaml || {
            echo "::error::Schema is stale — run 'make generate-types' to update"
            exit 1
          }
